import React from 'react';
import './style/sidebar.css';

const SideBar = ({ title }) => {
  return (
    <div class="sidenav">
      <a href="#">1</a>
      <a href="#">2</a>
      <a href="#">3</a>
      <a href="#">4</a>
    </div>
  );
};

export default SideBar;
